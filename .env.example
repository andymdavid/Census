# OpenRouter configuration for AI form generation.
# Copy this file to `.env` locally and replace the placeholder key.

OPENROUTER_API_KEY=replace_with_your_openrouter_api_key

# Optional. If omitted, the server uses its built-in default model.
OPENROUTER_MODEL=openai/gpt-4o-mini
